
name: Build modules

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/checkout@v3
      with:
        repository: micropython/micropython
        path: micropython
        ref: v1.20.0
    - name: Setup MicroPython X86
      working-directory: micropython
      run: source tools/ci.sh && ci_unix_32bit_setup && ci_unix_standard_build
    - name: Build module x64
      run: make -C eml_trees/ ARCH=x64 V=1 MPY_DIR=../micropython
    - name: Copy module x64
      run: mkdir -p dist && cp ./eml_trees/emltrees.mpy dist/
    - name: Archive production artifacts
      uses: actions/upload-artifact@v3
      with:
        name: dist-without-markdown
        path: |
          dist
    - name: Setup MicroPython ARM
      working-directory: micropython
      run: source tools/ci.sh && ci_rp2_setup



